import React from 'react'
import ReactDOM from 'react-dom/client'
import StreamerFormPage from './pages/StreamerFormPage.tsx'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <StreamerFormPage />
  </React.StrictMode>,
)
